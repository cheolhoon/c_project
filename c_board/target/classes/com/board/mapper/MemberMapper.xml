<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-maybatis.org//DTD Mapper 3.0//EN"
"http://maybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.board.mapper.MemberMapper">

	<insert id="memberJoin">
		INSERT INTO BOOK_MEMBER (	MEMBERID
									, MEMBERPW
									, MEMBERNAME
									, MEMBERMAIL
									, MEMBERADDR1
									, MEMBERADDR2
									, MEMBERADDR3
									, ADMINCK
									, REGDATE
									, MONEY
									, POINT )
						VLAUES (	#{memberId}
									, #{memberPw}
									, #{memberName}
									, #{memberMail}
									, #{memberAddr1}
									, #{memberAddr2}
									, #{memberAddr3}
									, #{adminCk}
									, #{regDate}
									, #{money}
									, #{point} )
	</insert>

</mapper>